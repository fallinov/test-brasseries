{
  "name": "brasseries-de-suisse",
  "private": true,
  "version": "0.1.0",
  "description": "Carte interactive des brasseries de Suisse — projet pédagogique C822.",
  "scripts": {
    "dev": "npx live-server --no-browser --port=8080",
    "test": "playwright test",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
